---
title: "group_work"
output: html_document
---
Lecture 1: Kmeans clustering and Hierarchical Clustering (HC) & Heat Maps

Install packages
```{r}
library("corrplot")
library("gplots")
library("kohonen")
library("plot3D")
library("factoextra")
library("stringr")
library("dplyr")
```

Data preparation:
```{r}
# clean the memory
rm(list = ls(all = TRUE)) 

# load data
df <- read.csv("/Users/marcelulrich/Documents/Master of Science in Applied Information and Data Science/3. Semester/Machine Learning/Gruppenarbeit/winequality-red.csv")

# count the missing values (NA) in the data frame
sum(is.na(df))

# check if the columns are numeric
sapply(df, is.numeric)

```

```{r}
# scale the data
df_scaled <- scale(df)

df_scaled

```



